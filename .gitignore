# Secrets — never commit
.env

# Data and run artifacts — root-anchored: src/prime_radiant/epi/data is CODE
/data/
/model-output/
*.duckdb
logs/
.coverage

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
dist/

# OS
.DS_Store

# MkDocs build output
site/

# Shadow-run scratch (only the validated CSV in shadow-output/ is committed)
shadow-scratch/

# social clips rendered by scripts/record_sentinel.py — regenerate, do not commit
reports/*.mp4
